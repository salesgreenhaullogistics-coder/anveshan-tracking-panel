# ── Filflo Bot — Original dependencies ──
selenium>=4.10.0
openpyxl>=3.1.0

# ── NLP Bridge — New dependencies ──
groq>=0.12.0          # FREE LLM backend (Groq Cloud)
openai>=1.40.0        # Alternative backend (OpenAI GPT)
